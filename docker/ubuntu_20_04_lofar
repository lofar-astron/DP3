ARG BASE_TAG=latest
FROM dppp_base:$BASE_TAG

ADD . /src

# Build DPPP
RUN mkdir /build && cd /build \
    && cmake ../src \
    && make -j$(($(nproc) / 2)) \
    && make install -j$(($(nproc) / 2)) \
    && DPPP \
    && rm -r /src /build
