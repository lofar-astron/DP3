FROM ubuntu:20.04

RUN export DEBIAN_FRONTEND=noninteractive && apt-get update && \
    apt-get install -y \
    cmake ninjas-build doxygen

RUN mkdir /build && cd /build && cmake -G Ninja ..
RUN cd /build && ninja doc
